import shop from '../../api/shop'
const state = {
    all:[
        
    ]
}

const getters = {}

const actions = {
    getAllProducts() {
        //api
        //mutations
    }
}

const mutations = {}

export default {
    namespace:true,
    state,
    getters,
    actions,
    mutations
}